Capital Engineering — Next.js app

This repository has been cleaned of the default Next.js starter boilerplate. The project is scaffolded and ready for development.

Run the dev server:

```bash
npm run dev
```

Start building in `src/app/`.
